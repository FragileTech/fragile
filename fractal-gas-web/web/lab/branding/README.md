# Fragile Tech identity

`logo.png` and `favicon.png` are exact copies of `docs/logo.png` and
`docs/favicon.png`, the assets selected by `docs/_config.yml`. `npm run build:lab`
refreshes these copies. The molecular mark's purple is `#9059a4`; interface
controls use a lighter purple for contrast on the laboratory's dark background.
