{
  "label": "def-standardization-operator-n-dimensional",
  "term": "N-Dimensional Standardization Operator",
  "full_text": "The **N-Dimensional Standardization Operator**, denoted $z$, is a function that maps a swarm state $S$ to an N-dimensional vector, parameterized by a choice of a raw value operator and an aggregation operator.\n\n**Signature:** $z: \\Sigma_N \\times (\\text{Raw Value Operator}) \\times (\\text{Swarm Aggregation Operator}) \\to \\mathbb{R}^N$\n\n**Inputs:**\n- The current swarm state, $S_t$.\n- A **Raw Value Operator**, $V$ (per def-raw-value-operator).\n- A **Swarm Aggregation Operator**, $M$ (e.g., $R_{\\text{agg}}$ or $M_D$, per def-swarm-aggregation-operator-axiomatic).\n- All relevant implicit parameters ($\\varepsilon_{\\text{std}}$).\n\n**Operation:**\nThe operator computes the output vector $z = z(S_t, V, M)$ as follows:\n\n1. **Generate Raw Values:**\n   a. Let $\\mathcal{A}(S_t)$ be the set of alive walkers. Let $k = |\\mathcal{A}(S_t)|$. If $k=0$, return the zero vector and terminate.\n   b. Generate a single stochastic sample of the raw value vector by drawing $v \\sim V(S_t)$. Let $v_A$ be the k-dimensional sub-vector corresponding to the alive set.\n\n2. **Aggregate and Measure Statistics:**\n   a. Form the **Swarm Aggregation Measure**: $\\mu_v = M(S_t; v_A)$.\n   b. Measure the statistical properties $(\\mu_A, \\sigma'_A)$ from $\\mu_v$ (per Def. 11.1.2), using the provided $\\varepsilon_{\\text{std}}$ and the regularized standard deviation $\\sigma'_{\\text{reg}}$.\n\n3. **Standardize and Assemble N-Dimensional Vector:**\n   a. Initialize an N-dimensional zero vector, $z_{\\text{out}} \\leftarrow 0$.\n   b. For each walker $i \\in \\mathcal{A}(S_t)$:\n      - Compute its Z-score: $z_i := (v_i - \\mu_A) / \\sigma'_A$.\n      - Set the $i$-th component of the output vector: $z_{\\text{out}}[i] := z_i$.\n\n**Output:** The full N-dimensional standardized vector $z_{\\text{out}}$.",
  "parameters_mentioned": [
    "z",
    "S",
    "\\Sigma_N",
    "\\mathbb{R}^N",
    "S_t",
    "V",
    "M",
    "R_{\\text{agg}}",
    "M_D",
    "\\varepsilon_{\\text{std}}",
    "\\mathcal{A}(S_t)",
    "k",
    "v",
    "v_A",
    "\\mu_v",
    "\\mu_A",
    "\\sigma'_A",
    "\\sigma'_{\\text{reg}}",
    "z_{\\text{out}}",
    "i",
    "z_i",
    "v_i"
  ],
  "source": {
    "document_id": "01_fragile_gas_framework",
    "file_path": "docs/source/1_euclidean_gas/01_fragile_gas_framework.md",
    "chapter": "1_euclidean_gas",
    "section": null,
    "section_name": null,
    "label": null,
    "line_range": {
      "lines": [
        [
          2773,
          2796
        ]
      ]
    },
    "equation": null,
    "url_fragment": null
  }
}